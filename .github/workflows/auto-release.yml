name: Auto Release

on:
  push:
    branches:
      - main
    paths:
      - 'gemini-extension.json'

permissions:
  contents: write

jobs:
  release:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 2

      - name: Check version change
        id: check
        run: |
          # Extract version from current file
          NEW_VER=$(jq -r .version gemini-extension.json)
          
          # Extract version from previous commit
          # If this is the first commit or file didn't exist, handle gracefully
          if git show HEAD^:gemini-extension.json > /dev/null 2>&1; then
            OLD_VER=$(git show HEAD^:gemini-extension.json | jq -r .version)
          else
            OLD_VER="0.0.0"
          fi
          
          echo "Old: $OLD_VER, New: $NEW_VER"
          
          if [ "$OLD_VER" != "$NEW_VER" ]; then
            echo "Version changed!"
            echo "changed=true" >> $GITHUB_OUTPUT
            echo "version=$NEW_VER" >> $GITHUB_OUTPUT
          else
            echo "Version unchanged."
            echo "changed=false" >> $GITHUB_OUTPUT
          fi

      - name: Create Release
        if: steps.check.outputs.changed == 'true'
        uses: softprops/action-gh-release@v1
        with:
          tag_name: v${{ steps.check.outputs.version }}
          name: v${{ steps.check.outputs.version }}
          generate_release_notes: true
          token: ${{ secrets.GITHUB_TOKEN }}
